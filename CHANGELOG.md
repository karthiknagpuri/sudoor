# Changelog

## 1.0 — 2026-06-16

First release.

- Dynamic Island permission prompt at the MacBook notch (genie-in / genie-out).
- `PermissionRequest` hook for Claude Code — Approve / Disapprove / defer-to-CLI.
- Source tagging: `app · project · tty`; raises the exact requesting window.
- Per-window raise + minimize for Terminal & iTerm (Automation) and Cursor & VS Code (Accessibility).
- Menu bar agent: alien mark, blinking dot when waiting, live "Requesting now" list, recent history, handled counter.
- `SMAppService` login item; atomic flock-protected state in `~/.island/config.json`.
- Landing page at sudoor.bar.
